# .github/workflows/deploy-site.yml
# Builds the Astro site and deploys to GitHub Pages.
# Also copies the apt repo tree (managed by build-deb.yml) into the Pages artifact.

name: Deploy Site

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: site/package-lock.json

      - name: Install dependencies
        working-directory: site
        run: npm ci

      - name: Build site
        working-directory: site
        run: npm run build

      - name: Copy apt repo tree into Pages artifact
        run: |
          if [ -d "apt-repo/public" ]; then
            mkdir -p dist/apt
            cp -r apt-repo/public/* dist/apt/
            echo "apt repo tree copied to dist/apt/"
          else
            echo "no apt-repo/public/ found, skipping apt repo in this deploy"
          fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
